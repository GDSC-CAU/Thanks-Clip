import { letter } from "./letter"
import { myName } from "./name"
import { activeSticker, stickers } from "./sticker"

const store = {
    letter,
    myName,
    stickers,
    activeSticker,
}

export { store }
